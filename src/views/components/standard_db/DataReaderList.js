import React from 'react'
import { connect } from 'react-redux'

export const DataReaderList = (props) => {
  return (
    <div>DataReaderList</div>
  )
}

const mapStateToProps = (state) => ({})

const mapDispatchToProps = {}

export default connect(mapStateToProps, mapDispatchToProps)(DataReaderList)