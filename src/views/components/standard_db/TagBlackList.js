import React from 'react'
import { connect } from 'react-redux'

export const TagBlackList = (props) => {
  return (
    <div>TagBlackList</div>
  )
}

const mapStateToProps = (state) => ({})

const mapDispatchToProps = {}

export default connect(mapStateToProps, mapDispatchToProps)(TagBlackList)