import React from 'react'
import { connect } from 'react-redux'

export const ReaderDeviceList = (props) => {
  return (
    <div>ReaderDeviceList</div>
  )
}

const mapStateToProps = (state) => ({})

const mapDispatchToProps = {}

export default connect(mapStateToProps, mapDispatchToProps)(ReaderDeviceList)